function insertUsers(usersHTML) {
  const queryList = document.querySelector('.query-list');
  queryList.innerHTML = "";
  queryList.insertAdjacentHTML('beforeend', usersHTML);
}

insertUsers("<%= j render 'pages/user_index', users: @users %>");
